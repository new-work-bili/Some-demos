import React, { PureComponent } from "react";
import FliterList from "../connet/filterListConnet";

export default class Home extends PureComponent {
    render() {
        return (
            <div>
                <FliterList></FliterList>
            </div>
        );
    }
}
